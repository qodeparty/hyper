/*/////////////////////////////////////////////////////////////////////////////

  Author  : Avi Damasi

/////////////////////////////////////////////////////////////////////////////*/

/*/////////////////////////////////////////////////////////////////////////////
// - DATE=>%date% BUILD=>%build% VERSION=>%version% BID=>%bid:123%
/////////////////////////////////////////////////////////////////////////////*/

  "use strict";

/*/////////////////////////////////////////////////////////////////////////////
// Δ Includes
/////////////////////////////////////////////////////////////////////////////*/

  const HyperServer = require('./nox');
  const config    = NoxServer.defaults;

/*/////////////////////////////////////////////////////////////////////////////
// Run
/////////////////////////////////////////////////////////////////////////////*/

  try{
    console.log("================== " + config.SERVER_ID.toUpperCase() +" STARTED ===================");
    let hyper = HyperServer({ gx:require('./gx'), router:require('./router') });
  }catch(err){
    console.error(err);
    process.exit(1);
  }
